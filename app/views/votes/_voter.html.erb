<% if policy( Vote.new ).create? %>
  <div class="vote-arrows pull-left">
    <div>
      <%= up_vote_link_classes(topic, post) %>
    </div>
    <div>
      <strong><%= post.points %></strong>
    </div>
    <div>
      <%= down_vote_link_classes(topic, post) %>
    </div>
  </div>
<% end %>